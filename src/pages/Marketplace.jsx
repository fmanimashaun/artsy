import React from 'react';
import Header from 'components/Header';

const MarketPlace = () => {
  const message = 'Marketplace page';
  return (
    <>
      <Header />
      <h1>{message}</h1>
    </>
  );
};

export default MarketPlace;
